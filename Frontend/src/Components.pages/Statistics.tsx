import React from "react";
import { useStyles } from "../theme";

function Statistics(): JSX.Element {
    return (
        <div className={useStyles().defaultStyle} style={{ minHeight: '200px' }}>
			Coming soon...
        </div>
    );
}

export default Statistics;
